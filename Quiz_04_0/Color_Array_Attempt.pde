//DOEST CHANGE COLOR
//color[] buttonCol = {color(255, 0, 0),color(0, 255, 0), color(0, 0, 255)};

//void setup() {
//  size(1000, 800);
  
  //buttonCol = new color[3];
  //buttonCol[0] = color(255, 0, 0);
  //buttonCol[1] = color(0, 255, 0);
  //buttonCol[2] = color(0, 0, 255);

  //for (int i = 0; i < buttonCol.length; i++) {
    //buttonCol[i] = 0;
//  }
//}

//void draw() {
//background(0);
//  if (dist(mouseX, mouseY, width/2, height/2) < 50) {
//    stroke(255, 255, 10);
//  } else {
//    stroke(0);
//  }
  
//for (int i = 0; i < buttonCol.length; i++){
  //fill(buttonCol[i]);
//}

//  ellipse(width/2, height/2, 100, 100);
//}

//void mouseClicked() {
//  if (dist(mouseX, mouseY, width/2, height/2) < 50) {
//    for (int i = 0; i < buttonCol.length; i++) {
//      i += 1;
      
//      if(i <= buttonCol.length - 1){
//       i = 0; 
//      }
    
//    }
//  }
//}
